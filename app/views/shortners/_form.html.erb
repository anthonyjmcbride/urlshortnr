<%= form_with(model: shortner, local: true) do |form| %>
  <% if shortner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shortner.errors.count, "error") %> prohibited this shortner from being saved:</h2>

      <ul>
      <% shortner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :longurl %>
    <%= form.text_field :longurl, id: :shortner_longurl %>
  </div>

  <div class="field">
    <%= form.label :shorturl %>
    <%= form.text_field :shorturl, id: :shortner_shorturl %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
